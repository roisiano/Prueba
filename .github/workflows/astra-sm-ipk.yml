name: Build astra-sm for CoreELEC Entware (aarch64-k3.10)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Install host dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk \
                              gcc-multilib g++-multilib gettext git libncurses5-dev \
                              libssl-dev python3-distutils rsync unzip zlib1g-dev \
                              file wget

      - name: Clone Entware (correct branch)
        run: |
          git clone -b entware-21.02 https://github.com/Entware/Entware.git
          cd Entware
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure for aarch64-k3.10
        run: |
          cd Entware
          cp configs/aarch64-k3.10.config .config
          make defconfig

      - name: Add astra-sm package
        run: |
          cd Entware
          mkdir -p package/astra-sm

          cat > package/astra-sm/Makefile << 'EOF'
          include $(TOPDIR)/rules.mk

          PKG_NAME:=astra-sm
          PKG_VERSION:=0.2.88
          PKG_RELEASE:=1

          PKG_SOURCE_PROTO:=git
          PKG_SOURCE_URL:=https://github.com/crazycat69/astra-sm.git
          PKG_SOURCE_VERSION:=master
          PKG_MIRROR_HASH:=skip

          include $(INCLUDE_DIR)/package.mk

          define Package/astra-sm
            SECTION:=net
            CATEGORY:=Network
            TITLE:=Astra Streaming Server
            DEPENDS:=+libgcc
          endef

          define Build/Compile
            $(MAKE) -C $(PKG_BUILD_DIR)/src \
              CC="$(TARGET_CC)" \
              CXX="$(TARGET_CXX)"
          endef

          define Package/astra-sm/install
            $(INSTALL_DIR) $(1)/opt/bin
            $(INSTALL_BIN) $(PKG_BUILD_DIR)/src/astra $(1)/opt/bin/astra-sm
          endef

          $(eval $(call BuildPackage,astra-sm))
          EOF

      - name: Build astra-sm
        run: |
          cd Entware
          make package/astra-sm/compile V=s -j$(nproc)

      - name: Upload generated IPK
        uses: actions/upload-artifact@v4
        with:
          name: astra-sm-aarch64-k3.10
          path: Entware/bin/packages/aarch64-k3.10/base/astra-sm*.ipk
